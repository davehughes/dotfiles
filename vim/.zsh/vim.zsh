alias :e=$EDITOR
alias :q="exit"

# HACK: on OSX, /usr/bin/vim can't be moved and gets in the way of homebrew's version. This fixes that without
# having to perform PATH surgery.
alias vim=/opt/homebrew/bin/vim
